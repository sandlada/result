import { describe, it, expect } from 'vitest';import { asyncOk, asyncErr, orElseAsync } from '../../src/index.js';describe('orElseAsync', () => {    it('recovers from failure (curried)', async () => {        const recover = orElseAsync(() => asyncOk(42));        const r = await recover(asyncErr<string>('down'));        expect(r.isSuccess).toBe(true);        if (r.isSuccess) expect(r.value).toBe(42);    });    it('passes through success', async () => {        const r = await orElseAsync(() => asyncOk(99), asyncOk(10));        expect(r.isSuccess).toBe(true);        if (r.isSuccess) expect(r.value).toBe(10);    });});
