import { describe, it, expect } from 'vitest';import { asyncOk, asyncErr, tapAsync } from '../../src/index.js';describe('tapAsync', () => {    it('calls side-effect on success', async () => {        let side = 0;        const r = await tapAsync((v: number) => { side = v; }, asyncOk(5));        expect(side).toBe(5);        expect(r.isSuccess).toBe(true);        if (r.isSuccess) expect(r.value).toBe(5);    });    it('does not call on failure', async () => {        let side = 0;        await tapAsync((v: number) => { side = v; }, asyncErr<string>('err'));        expect(side).toBe(0);    });});
