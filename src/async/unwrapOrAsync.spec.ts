import { describe, it, expect } from 'vitest';import { asyncOk, asyncErr, unwrapOrAsync } from '../../src/index.js';describe('unwrapOrAsync', () => {    it('returns value on success', async () => {        const v = await unwrapOrAsync(0, asyncOk(42));        expect(v).toBe(42);    });    it('returns default on failure', async () => {        const v = await unwrapOrAsync(99, asyncErr<string>('err'));        expect(v).toBe(99);    });});
