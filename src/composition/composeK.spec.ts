import { describe, it, expect } from 'vitest';import type { IResultOfT } from '../../src/types/IResultOfT.js';import { ok, err, composeK } from '../../src/index.js';describe('composeK', () => {    type AppErr = string;    const parse = (input: string): IResultOfT<number, AppErr> => {        const n = Number(input);        return Number.isNaN(n) ? err<AppErr>('Not a number') : ok(n);    };    const double = (n: number): IResultOfT<number, AppErr> => ok(n * 2);    it('chains two successful functions', () => {        const composed = composeK(parse, double);        const result = composed('21');        expect(result.isSuccess).toBe(true);        if (result.isSuccess) expect(result.value).toBe(42);    });    it('short-circuits when the first function fails', () => {        let secondCalled = false;        const tracking = (n: number): IResultOfT<number, AppErr> => {            secondCalled = true;            return ok(n * 2);        };        const composed = composeK(parse, tracking);        const result = composed('invalid');        expect(secondCalled).toBe(false);        expect(result.isSuccess).toBe(false);        if (!result.isSuccess) expect(result.error).toBe('Not a number');    });    it('chains to failure when the second function fails', () => {        const failIfLarge = (n: number): IResultOfT<number, AppErr> =>            n > 10 ? err<AppErr>('Too large') : ok(n);        const composed = composeK(parse, failIfLarge);        const result = composed('21');        expect(result.isSuccess).toBe(false);        if (!result.isSuccess) expect(result.error).toBe('Too large');    });    it('supports nested composeK', () => {        const addOne = (n: number): IResultOfT<number, AppErr> => ok(n + 1);        const composed = composeK(parse, composeK(double, addOne));        const result = composed('21');        expect(result.isSuccess).toBe(true);        if (result.isSuccess) expect(result.value).toBe(43);    });    it('catches sync throw from first function', () => {        const throwing = (_x: string): any => { throw new Error('boom'); };        const composed = composeK(throwing, double);        const result = composed('hello');        expect(result.isFailure).toBe(true);        if (result.isFailure) expect(result.error).toBeInstanceOf(Error);    });    it('catches sync throw from second function', () => {        const throwing = (_x: number): any => { throw new RangeError('overflow'); };        const composed = composeK(parse, throwing);        const result = composed('21');        expect(result.isFailure).toBe(true);        if (result.isFailure) expect(result.error).toBeInstanceOf(RangeError);    });});

