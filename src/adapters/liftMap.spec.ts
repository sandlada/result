import { describe, it, expect } from 'vitest';import { ok, err, liftMap } from '../../src/index.js';describe('liftMap', () => {    const double = (x: number) => x * 2;    it('curried: liftMap(fn) returns a function that maps success', () => {        const lifted = liftMap(double);        const result = lifted(ok(21));        expect(result.isSuccess).toBe(true);        if (result.isSuccess) expect(result.value).toBe(42);    });    it('direct: liftMap(fn, ok(value)) transforms', () => {        const result = liftMap(double, ok(21));        expect(result.isSuccess).toBe(true);        if (result.isSuccess) expect(result.value).toBe(42);    });    it('failure passes through', () => {        const result = liftMap(double, err<string>('bad'));        expect(result.isSuccess).toBe(false);        if (!result.isSuccess) expect(result.error).toBe('bad');    });});
