import { describe, it, expect } from 'vitest';import { teeAsync } from '../../src/index.js';describe('teeAsync', () => {    it('calls async side-effect and returns input', async () => {        let side = '';        const log = teeAsync(async (s: string) => {            side = s;        });        const result = await log('data');        expect(side).toBe('data');        expect(result).toBe('data');    });    it('propagates when f rejects (one-track, no railway)', async () => {        const log = teeAsync(async () => { throw new Error('boom'); });        await expect(log(42)).rejects.toThrow('boom');    });});
