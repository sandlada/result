import { describe, it, expect } from 'vitest';import { ok, err } from '../../src/index.js';import type { IResultOfT } from '../../src/types/IResultOfT.js';import { and, unwrap } from '../../src/index.js';describe('and', () => {    it('direct form', () => {        const a: IResultOfT<number> = ok(1);        const b: IResultOfT<string> = ok('ok');        expect(unwrap(and(b, a))).toBe('ok');    });    it('curried form', () => {        const andWith = and(ok('result'));        expect(unwrap(andWith(ok(5)))).toBe('result');        expect(andWith(err<number>(new Error('no'))).isSuccess).toBe(false);    });});

