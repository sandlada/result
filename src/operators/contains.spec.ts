import { describe, it, expect } from 'vitest';import { ok, err } from '../factories/index.js';import { contains } from './index.js';describe('contains', () => {    it('curried form', () => {        const isFortyTwo = contains(42);        expect(isFortyTwo(ok(42))).toBe(true);        expect(isFortyTwo(ok(7))).toBe(false);        expect(isFortyTwo(err<number>(new Error('err')))).toBe(false);    });});

