import { describe, it, expect } from 'vitest';import { ok, err } from '../../src/index.js';import type { IResultOfT } from '../../src/types/IResultOfT.js';import { swap, unwrap } from '../../src/index.js';describe('swap', () => {    it('success becomes failure', () => {        const r: IResultOfT<string, number> = ok('hello');        const swapped = swap(r);        expect(swapped.isSuccess).toBe(false);        expect(swapped.isFailure).toBe(true);    });    it('failure becomes success', () => {        const r: IResultOfT<string, number> = err<string, number>(404);        const swapped = swap(r);        expect(swapped.isSuccess).toBe(true);        expect(unwrap(swapped)).toBe(404);    });});

