import { describe, it, expect } from 'vitest';import { ok, err, match } from '../../src/index.js';describe('match', () => {    const onOk = (v: number) => `value: ${v}`;    const onErr = (e: string) => `error: ${e}`;    it('curried: match(onOk, onErr) applied to success', () => {        const matcher = match(onOk, onErr);        expect(matcher(ok(42))).toBe('value: 42');    });    it('curried: match(onOk, onErr) applied to failure', () => {        const matcher = match(onOk, onErr);        expect(matcher(err('bad'))).toBe('error: bad');    });    it('direct: match(onOk, onErr, ok(value))', () => {        expect(match(onOk, onErr, ok(42))).toBe('value: 42');    });    it('direct: match(onOk, onErr, err(error))', () => {        expect(match(onOk, onErr, err('bad'))).toBe('error: bad');    });    it('onOk and onErr can return different types — TS infers union', () => {        const result = match(            (_v: number): 123 | 'error' => 123 as const,            (_e: string): 123 | 'error' => 'error' as const,            ok(42),        );        expect(result).toBe(123);    });});

