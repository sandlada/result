import { describe, it, expect } from 'vitest';import { ok, err, unwrapOr } from '../../src/index.js';describe('unwrapOr', () => {    it('curried: returns value on success', () => {        const extractor = unwrapOr(0);        expect(extractor(ok(42))).toBe(42);    });    it('direct: returns value on success', () => {        expect(unwrapOr(0, ok(42))).toBe(42);    });    it('curried: returns default on failure', () => {        const extractor = unwrapOr(0);        expect(extractor(err<string>('bad'))).toBe(0);    });    it('direct: returns default on failure', () => {        expect(unwrapOr(0, err('bad'))).toBe(0);    });});

