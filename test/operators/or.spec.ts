import { describe, it, expect } from 'vitest';import { ok, err } from '../../src/index.js';import type { IResultOfT } from '../../src/types/IResultOfT.js';import { or, unwrap } from '../../src/index.js';describe('or', () => {    it('direct form', () => {        const fallback: IResultOfT<number> = ok(7);        const failed: IResultOfT<number> = err<number>(new Error('oops'));        expect(unwrap(or(fallback, failed))).toBe(7);    });    it('success passes through', () => {        const success: IResultOfT<number> = ok(3);        const fallback: IResultOfT<number> = ok(99);        expect(unwrap(or(fallback, success))).toBe(3);    });});

