import { describe, it, expect } from 'vitest';import { ok, err } from '../../src/index.js';import type { IResultOfT } from '../../src/types/IResultOfT.js';import { bimap, unwrap } from '../../src/index.js';describe('bimap', () => {    it('direct form', () => {        const r: IResultOfT<number, Error> = ok(3);        const result = bimap(            (v: number) => String(v),            (e: Error) => e.message,            r,        );        expect(unwrap(result)).toBe('3');    });    it('curried form', () => {        const transform = bimap(            (v: number) => v + 1,            (e: Error) => e.message,        );        expect(unwrap(transform(ok(1)))).toBe(2);        expect(transform(err<number>(new Error('fail'))).isSuccess).toBe(false);    });});

