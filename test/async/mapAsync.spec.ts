import { describe, it, expect } from 'vitest';import { asyncOk, asyncErr, mapAsync } from '../../src/index.js';describe('mapAsync', () => {    it('maps success value (curried)', async () => {        const double = mapAsync((x: number) => x * 2);        const r = await double(asyncOk(21));        expect(r.isSuccess).toBe(true);        if (r.isSuccess) expect(r.value).toBe(42);    });    it('maps success value (direct)', async () => {        const r = await mapAsync((x: number) => x * 2, asyncOk(21));        expect(r.isSuccess).toBe(true);        if (r.isSuccess) expect(r.value).toBe(42);    });    it('passes through failure', async () => {        const r = await mapAsync((x: number) => x * 2, asyncErr<string>('fail'));        expect(r.isFailure).toBe(true);        if (r.isFailure) expect(r.error).toBe('fail');    });    it('maps with async callback (curried)', async () => {        const doubleAsync = mapAsync(async (x: number) => x * 2);        const r = await doubleAsync(asyncOk(21));        expect(r.isSuccess).toBe(true);        if (r.isSuccess) expect(r.value).toBe(42);    });    it('catches callback exceptions', async () => {        const r = await mapAsync(async () => { throw 'callback err'; }, asyncOk(1));        expect(r.isFailure).toBe(true);        if (r.isFailure) expect(r.error).toBe('callback err');    });});
