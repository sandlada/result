import { describe, it, expect } from 'vitest';import { tee } from '../../src/index.js';describe('tee', () => {    it('calls side-effect and returns the original value', () => {        let side: number | undefined;        const withLog = tee((x: number) => { side = x; });        const result = withLog(42);        expect(side).toBe(42);        expect(result).toBe(42);    });    it('does not mutate the value', () => {        let caught: string | undefined;        const log = tee((x: string) => { caught = x; });        const result = log('hello');        expect(caught).toBe('hello');        expect(result).toBe('hello');    });});
