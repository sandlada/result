import { describe, it, expect } from 'vitest';import { teeAsync } from '../../src/index.js';describe('teeAsync', () => {    it('calls async side-effect and returns input', async () => {        let side = '';        const log = teeAsync(async (s: string) => {            side = s;        });        const result = await log('data');        expect(side).toBe('data');        expect(result).toBe('data');    });});
