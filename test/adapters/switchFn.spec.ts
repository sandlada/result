import { describe, it, expect } from 'vitest';import { ok, err, switchFn } from '../../src/index.js';describe('switchFn', () => {    it('wraps a normal function to return a success result', () => {        const safeParseInt = switchFn((s: string) => Number.parseInt(s, 10));        const result = safeParseInt('42');        expect(result.isSuccess).toBe(true);        if (result.isSuccess) expect(result.value).toBe(42);    });    it('exception propagates (switchFn does not catch)', () => {        const badFn = switchFn((_s: string) => {            throw new Error('unexpected');        });        expect(() => badFn('anything')).toThrow('unexpected');    });    it('preserves falsy return values', () => {        const returnFalse = switchFn((_x: unknown) => false);        const result = returnFalse(undefined);        expect(result.isSuccess).toBe(true);        if (result.isSuccess) expect(result.value).toBe(false);    });    it('preserves null return values', () => {        const returnNull = switchFn((_x: unknown) => null);        const result = returnNull(undefined);        expect(result.isSuccess).toBe(true);        if (result.isSuccess) expect(result.value).toBeNull();    });});

