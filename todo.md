# todo

> 项目级"待办"清单。基于源码静态推断(未读 spec 文件)。
> 所有判断依据: AGENTS.md / ARCH.md / SPEC.md 三份规范,以及 `.jules/sentinel.md` 已记录的历史事故。
>
> **风险维度**:
> - **D1** 项目规范 (AGENTS.md / ADR 1-6) — 工厂返回最窄类型;`value` / `error` 仅在判别分支存在;`readonly`;`import type`;无类。
> - **D2** 项目核心思想 (ARCH.md / ADR 1-6) — 纯对象不装箱;data-last 柯里化;无 barrel 循环;独立 Option。
> - **D3** 历史事故模式 (`.jules/sentinel.md`) — 生成器 `iterator.return()` 资源泄漏;`in` 在非对象上抛 `TypeError`;JSON 重水合丢字面量类型。
> - **D4** 业界基准 (FP / TS 类型设计最佳实践) — 错误透传不被静默吞;侧效应可见;序列化可逆;泛型尽量早推断。
>
> **当前标注**: `* @note Ready for Product` 为项目既定 JSDoc 契约,`未标记/仅类型` 表示 `src/types/` 下纯类型定义文件,无运行时可标注。
> **154 个源函数** (排除每个模块的 `index.ts` 与 `*.spec.ts`) 共覆盖 10 个子系统。


## [RESOLVED] factories (10)

| 函数              | 当前标注          | 风险维度 | 预期生产问题                                                                                                                                                                                                                                         | 推荐验证手段                                                                                                     |
| ----------------- | ----------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `ok` / `ok()`     | Ready for Product | D1       | `ok()` 与 `ok(undefined)` 通过 `arguments.length` 区分,严格模式下当 T 形参未被引用时 V8 可能消除对 `arguments` 的观测,导致 void 语义丢失;curried 双断言 `as unknown as IResultOfT<T, never>` 在 T 为 `never` 表达式命中底类型,跨模块边界类型窄化失效 | TS 编译 `noUncheckedIndexedAccess` + `tsgo` strict;断言 `expect(ok()).toEqual({isSuccess:true,isFailure:false})` |
| `err`             | Ready for Product | D3       | `Error` 子类经 `JSON.stringify` 后丢失 `name` / `stack` 字段;`structuredClone` 在 Node 17+ 仅支持 `Error` 子集。日志聚合或跨进程传输会丢诊断                                                                                                         | `JSON.parse(JSON.stringify(err(new DomainError('msg')))).name` 断言存在                                          |
| `fromPredicate`   | Ready for Product | D1       | 工厂直接调用形态预创建 `error` 引用,在闭包内复用同一对象,日志比较会误判为同一故障;curried 形态只 bind `pred`,`error` 留作固定值                                                                                                                      | 多次 `fromPredicate(p, e, v)` 失败时 `===` 引用对比检测                                                          |
| `fromThrowable`   | Ready for Product | D4       | 默认未提供 `errorFn` 时仅把 `unknown` 透传,业务语义里下游 `TError` 一致性被破坏;sideways: 同名 `fromThrowable(fn)` 多模块 import 时 TS 推断歧义                                                                                                      | 移除 `errorFn` 后断言结果为 `IResultOfT<A, unknown>`,下游不可 `instanceof` 检查                                  |
| `fromPromise`     | Ready for Product | D3       | 第三方 Promise 经 `unknown` 强转后,运行期未捕获 reject 会从 `try/catch` 逃出;`Symbol.toStringTag` 多态 Promise(Bluebird / when.js)行为不一致                                                                                                         | 用 bluebird 实例化测试 `fromPromise(bluebird.reject(...))`                                                       |
| `fromSafePromise` | Ready for Product | D3       | 类型擦除后传非 `Promise<T>` 实参,运行期 reject 不会进 `Err` 通道而逃逸为未处理 rejection                                                                                                                                                             | 注入非 Promise 拒绝值,监测 `unhandledRejection`                                                                  |
| `tryCatch`        | Ready for Product | D4       | 默认 `errorFn` 缺位 → 抛 `unknown` 上游,业务 `TError` 失败;同步 throw 与 sync return 在不同 V8 版本中 profile 不同                                                                                                                                   | 移除第二个参数后断言结果为 `IResultOfT<T, unknown>`                                                              |
| `tryCatchAsync`   | Ready for Product | D4       | 同 `tryCatch`,且异步 reject 双路径(微任务内 / 外)需明确;Promise 化后丢失原始栈                                                                                                                                                                       | 同步 throw vs reject 路径分别测试                                                                                |
| `asyncOk`         | Ready for Product | D4       | 立即 resolved 的 Promise 在工厂体内同步 throw 不会被 Promise 包装,需调用方对 `value` 求值的安全契约                                                                                                                                                  | `asyncOk(throwsSync())` 应被 Promise 捕获                                                                        |
| `asyncErr`        | Ready for Product | D4       | 同 `asyncOk`,对错误参数 `error` 的副作用未文档化                                                                                                                                                                                                     | `asyncErr(mayThrow())` 应被 Promise 包装                                                                         |


## adapters (7)

| 函数            | 当前标注          | 风险维度 | 预期生产问题                                                                                                                       | 推荐验证手段                                                  |
| --------------- | ----------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| `switchFn`      | Ready for Product | D2       | 同步回调 throw 会冒泡成未捕获,无 `try/catch` 包装,与 `map` 的契约不一致;curried 形态对部分应用后抛错不一致                         | `switchFn(fn)(x)` vs `map(fn)(x)` 异常透传对比                |
| `switchFnAsync` | Ready for Product | D3       | 异步回调 reject 时未保留原始错误对象,变为 `unknown` 透传;若回调返回 `Promise<never>` 推断退化为 `void`                             | `switchFnAsync(throwAsync)('x')` 错误结构断言                 |
| `liftMap`       | Ready for Product | D2       | 作为 `map` 的教学别名,但 SPEC.md / README 未显式列在 `adapters` API 表;导出符号与 `operators.map` 重名会造成主入口 barrel 解析冲突 | grep `liftMap` 在 SPEC 中的引用次数                           |
| `tee`           | Ready for Product | D2       | 回调 throw 冒泡,SPEC.md 未声明错误归约行为;与 `tap` 的契约不一致                                                                   | `tee(thrower)(x)` 异常路径                                    |
| `teeAsync`      | Ready for Product | D3       | 异步 reject 后无错误归约,行为与 `tapAsync` 不一致                                                                                  | `teeAsync(rejector)(x)` 异常路径                              |
| `toOption`      | Ready for Product | D2       | 失败结果转 None 时**丢弃 error**,日志聚合/告警丢失诊断信息,且签名无注释警告                                                        | `toOption(err(complex))` 后日志中应存在对 `.error` 的访问警告 |
| `fromOption`    | Ready for Product | D2       | 默认 `error` 是占位符,在多场景复用同一字符串字面量,日志聚合无法区分                                                                | 对同一 `ofNone()` 多处 `fromOption('x')` 引用相等性           |


## async (35)

| 函数                  | 当前标注          | 风险维度 | 预期生产问题                                                                                                                                                | 推荐验证手段                                                                 |
| --------------------- | ----------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `asyncBind`           | Ready for Product | D3       | 用 `run in next` 判断 Promise/AsyncResult 边界时,`null` / primitive 抛 `TypeError`(对应 `.jules/sentinel.md` 第二条);回调 throw 后 `Err` 通道保留 `unknown` | `asyncBind(fn, null)`, `asyncBind(fn, 42)` TS 编译 + 运行时 `try/catch` 包裹 |
| `asyncBindOption`     | Ready for Product | D3       | 同 `asyncBind`,目标类型为 `IOption`;回调 throw 映射为 `None` 丢弃 error 对象                                                                                | `asyncBindOption(fn, undefined)` 边界                                        |
| `asyncBindThrough`    | Ready for Product | D3       | 同 `asyncBind`,且保留原 `value` 时回调错误与原结果类型对齐不明;嵌套 Promise reject 链路上异步 throw 与同步 throw 双语义                                     | `asyncBindThrough(throwingAsync, okP)(...)` 错误结构                         |
| `asyncMap`            | Ready for Product | D3       | 同步 Result 入参 + 异步 fn 的返回类型在 curried 形态退化为 `Promise<IResultOfT<B, unknown>>` 时下游无法 `match` 精确                                        | `asyncMap(asyncFn, ok(42))` 推断对齐                                         |
| `asyncTap`            | Ready for Product | D3       | 异步 side-effect 回调 throw 时归约到 `Err`,错误透传丢失上下文                                                                                               | `asyncTap(throwingAsync, ok(1))` 错误                                        |
| `asyncTapErr`         | Ready for Product | D3       | 与 `asyncTap` 对偶,失败侧回调 reject 后错误结构                                                                                                             | `asyncTapErr(throwingAsync, err('x'))`                                       |
| `asyncTapOption`      | Ready for Product | D3       | 同 `asyncTap`,目标 `IOption`,`None` 侧回调 throw 归约一致性                                                                                                 | `asyncTapOption(throwingAsync, ofSome(1))`                                   |
| `bimapAsync`          | Ready for Product | D4       | 同时对成功 / 失败映射异步时,回调同步 throw 与 reject 双路径需要契约;`IResultOfT<B, F>` 在嵌套 await 后类型推导回退                                          | `bimapAsync(throwingAsync, throwingAsync)(okP)(...)`                         |
| `bindAsync`           | Ready for Product | D3       | `next in x` 触 `in` 操作符 `TypeError` 风险,callback 是 `Promise<IResultOfT>` / 同步 `IResultOfT` 双形态推断分歧                                            | `bindAsync(fn, undefined)` 边界                                              |
| `bindAsyncOption`     | Ready for Product | D3       | 同上,目标类型 `IOption`                                                                                                                                     | 边界注入 `null` / `undefined`                                                |
| `bindThroughAsync`    | Ready for Product | D3       | 异步 callback throw 后保留原 `value` 时类型推断退化为 `IResultOfT<A, F \| unknown>`                                                                         | `bindThroughAsync(throwingAsync, okP)(...)`                                  |
| `containsAsync`       | Ready for Product | D4       | `target` 比较使用 `Object.is` 还是 `===` 未声明,`NaN` / `-0` 行为依赖 JS 语义                                                                               | `containsAsync(NaN)` vs `containsAsync(0)`                                   |
| `containsAsyncOption` | Ready for Product | D4       | 同 `containsAsync`,目标类型 `IOption`                                                                                                                       | `containsAsyncOption(NaN, ofSome(NaN))`                                      |
| `existsAsync`         | Ready for Product | D3       | 回调 throw 时归约到 `false`,边界 reject 会被静默吞掉                                                                                                        | `existsAsync(throwingAsync, ok(1))` 行为                                     |
| `existsAsyncOption`   | Ready for Product | D3       | 同 `existsAsync`,目标 `IOption`                                                                                                                             | 边界同上                                                                     |
| `filterAsyncOption`   | Ready for Product | D3       | 异步 predicate throw 时静默映射 `None`,丢失错误对象;`null` / `undefined` 入参 `in` 操作符风险                                                               | `filterAsyncOption(throwingAsync, ofSomeP)(...)`                             |
| `filterOrElseAsync`   | Ready for Product | D3       | 异步 predicate + 异步 errFn 双路径 reject,失败归约语义不清;`undefined` / `null` 入参边界                                                                    | `filterOrElseAsync(throwingP, throwingE, okP)(...)`                          |
| `flattenAsync`        | Ready for Product | D4       | 嵌套 `Promise<IResultOfT<IResultOfT<A, E>, E>>` 展平为 `Promise<IResultOfT<A, E>>`,但若外层 failure 嵌套在外层 success,语义需确认                           | `flattenAsync(okP(ok(1)))` 与 `flattenAsync(okP(err('x')))`                  |
| `flattenAsyncOption`  | Ready for Product | D4       | 同上,目标 `IOption`                                                                                                                                         | 同上                                                                         |
| `mapAsync`            | Ready for Product | D3       | `next in x` 边界风险;callback 为 `Promise<U>` / 同步 `U` 双形态推断分歧                                                                                     | `mapAsync(fn, undefined)`, `mapAsync(fn, 42)`                                |
| `mapAsyncOption`      | Ready for Product | D3       | 同 `mapAsync`,目标 `IOption`;None 侧无 callback 调用但需明确语义                                                                                            | 边界同上                                                                     |
| `mapErrAsync`         | Ready for Product | D3       | 失败侧异步 errFn throw 时归约到原 error?未知;嵌套 reject 双通道                                                                                             | `mapErrAsync(throwingAsync, errP)`                                           |
| `mapOrAsync`          | Ready for Product | D4       | `def` 默认值与 `fn` callback 类型不同步(Sync / Async)时签名分歧                                                                                             | `mapOrAsync(0, asyncFn, okP)`                                                |
| `mapOrElseAsync`      | Ready for Product | D4       | `onErr` 与 `fn` 都是异步回调,各自 reject 双路径                                                                                                             | `mapOrElseAsync(throwingE, throwingF, okP)`                                  |
| `matchAsync`          | Ready for Product | D4       | onOk / onErr 异步 + 同步混用时返回类型推断退化为 `Promise<unknown>`                                                                                         | 混用实测                                                                     |
| `matchAsyncOption`    | Ready for Product | D4       | 同 `matchAsync`,目标 `IOption`                                                                                                                              | 同上                                                                         |
| `orElseAsync`         | Ready for Product | D3       | errFn 是 `Promise<IResultOfT>` / 同步 `IResultOfT` 双形态;`in` 边界                                                                                         | `orElseAsync(throwAsyncFn, errP)`                                            |
| `orElseAsyncOption`   | Ready for Product | D3       | 同 `orElseAsync`,目标 `IOption`                                                                                                                             | 同上                                                                         |
| `swapAsync`           | Ready for Product | D4       | 交换 Ok/Err 异步语义下需明确哪些 await 路径需要合并;`undefined` 输入边界                                                                                    | `swapAsync(undefined)`                                                       |
| `tapAsync`            | Ready for Product | D3       | 异步 side-effect reject 后映射 Err;若原 result 已 failure,行为需明确                                                                                        | `tapAsync(throwingAsync, errP)`                                              |
| `tapAsyncOption`      | Ready for Product | D3       | 同 `tapAsync`,目标 `IOption`                                                                                                                                | 同上                                                                         |
| `tapErrAsync`         | Ready for Product | D3       | 失败侧 reject 后透传;side-effect 嵌套错误栈丢失                                                                                                             | `tapErrAsync(throwingAsync, okP)`                                            |
| `unwrapOrAsync`       | Ready for Product | D4       | 默认值 / 异步 callback 类型不同步时签名分歧;原 result 已是 success 时 callback 不被调用但需明确                                                             | `unwrapOrAsync(0, asyncThrowing, okP)`                                       |
| `unwrapOrAsyncOption` | Ready for Product | D4       | 同 `unwrapOrAsync`,目标 `IOption`                                                                                                                           | 同上                                                                         |
| `unwrapOrElseAsync`   | Ready for Product | D4       | failure 侧 callback 异步 throw 时归约语义不清;`undefined` 入参边界                                                                                          | 边界注入                                                                     |


## async-option (15)

| 函数          | 当前标注          | 风险维度 | 预期生产问题                                                                                                                              | 推荐验证手段                                                          |
| ------------- | ----------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `bind`        | Ready for Product | D3       | thunk 同步 throw 后映射为 `None`,错误对象丢失;`run in thunk` 边界触 `TypeError` 风险                                                      | `bind(throwingFn, ofSome(1))` 与 `bind(throwingFn, null)`             |
| `contains`    | Ready for Product | D4       | 比较语义依赖 `Object.is` 还是 `===` 未声明,`NaN` / `-0` 边界行为                                                                          | `contains(NaN, ofSome(NaN))`                                          |
| `exists`      | Ready for Product | D3       | 同步 predicate throw 时映射 `false`,错误被静默吞                                                                                          | `exists(throwingPred, ofSome(1))`                                     |
| `filter`      | Ready for Product | D3       | predicate throw 时映射 `None`,错误丢失;`null` 入参需防御                                                                                  | `filter(throwingPred, ofSome(1))`                                     |
| `flatten`     | Ready for Product | D4       | 嵌套 `Option<Option<T>>` 展平,但若外层是 None,语义是否短路需确认                                                                          | `flatten(ofNone())`, `flatten(ofSome(ofSome(1)))`                     |
| `from`        | Ready for Product | D3       | thunk 同步 throw 后 Promise 状态不一致 — 同步冒泡 vs 异步 reject 双路径                                                                   | `from(() => { throw new Error('x') }).run()`                          |
| `fromOption`  | Ready for Product | D2       | 同步 `IOption` 提升为 AsyncOption 后,失败 / None 之间映射无错位 — 但若 Option 是 None,thunk 返回类型推断为 `IOption<T> \| undefined` 边界 | 类型测试断言提升前后等价                                              |
| `fromPromise` | Ready for Product | D3       | reject 时映射 `None`,错误对象丢失(同 `fromPromise` 但落到 Option);thunk 同步 throw 不被包装                                               | `fromPromise(rejectingP).run()`, `fromPromise(() => { throw }).run()` |
| `map`         | Ready for Product | D3       | 同步 fn throw 时映射 `None`;`None` 侧不调用 fn,但 `null` / `undefined` 入参需防御                                                         | 边界注入                                                              |
| `mapAsync`    | Ready for Product | D3       | 异步 fn reject 时映射 `None`;同步 throw 双通道                                                                                            | `mapAsync(rejectingP, ofSome(1))`                                     |
| `match`       | Ready for Product | D4       | onSome / onNone 同步 / 异步混用推断退化为 `unknown`;调用约定需明确文档                                                                    | 类型测试                                                              |
| `orElse`      | Ready for Product | D3       | None 侧 fn 同步 throw 后行为;失败映射语义需确认                                                                                           | `orElse(throwingFn, ofNone())`                                        |
| `tap`         | Ready for Product | D3       | Some 侧 side-effect throw 归约 `None`;若原来 None,行为需明确                                                                              | `tap(throwingFn, ofSome(1))`                                          |
| `tapAsync`    | Ready for Product | D3       | 异步 side-effect reject 归约 `None`;原结果 None 时不被调用,但文档约束                                                                     | `tapAsync(rejectingP, ofSome(1))`                                     |
| `unwrapOr`    | Ready for Product | D4       | 默认值类型与 `T` 类型不协变时 `never` 路径无歧义;curried 形态推断需保留                                                                   | 类型测试                                                              |


## async-result (26)

| 函数                   | 当前标注          | 风险维度 | 预期生产问题                                                                                                                               | 推荐验证手段                                  |
| ---------------------- | ----------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------- |
| `andTee`               | Ready for Product | D3       | 同步 / 异步 side-effect 混用,reject 时归约 Err;原 result 已是 Err 时 callback 是否被调用需明确                                             | `andTee(throwingAsync, okAR)(...)`            |
| `andThrough`           | Ready for Product | D3       | 与 `andTee` 区别在于 prop 错误通道 — fn 失败时 narrow 到新 Err;sync throw 与 reject 双路径                                                 | `andThrough(throwingAsync, okAR)(...)`        |
| `bimap`                | Ready for Product | D4       | okFn / errFn 同步 / 异步混用;两者都 throw 时归约优先级需明确                                                                               | `bimap(throwingOk, throwingErr)(ar)`          |
| `bind`                 | Ready for Product | D3       | callback 是 `Promise<IResultOfT>` / 同步 `IResultOfT`,`run in` 边界;throw 后归约 Err 丢失上下文                                            | `bind(throwingFn, okAR)(...)`                 |
| `combine`              | Ready for Product | D2       | 数组内任一 `AsyncResult` failure 后短路,剩余 thunk 是否仍可能竞争 run() 需确认                                                             | 时序 race 测试                                |
| `combineWithAllErrors` | Ready for Product | D2       | 所有 thunk 都被执行时无并发上限声明;失败数组无深度限制;内存无上限                                                                          | 1000 失败项的累积测试                         |
| `contains`             | Ready for Product | D4       | 比较语义未声明,`run()` 后的 `Object.is` / `===` 行为                                                                                       | `contains(NaN, of(NaN))`                      |
| `exists`               | Ready for Product | D3       | 同步 predicate throw 归约 false,async reject 同样归约 false                                                                                | 边界测试                                      |
| `filterOrElse`         | Ready for Product | D3       | 同步 / 异步 predicate + errFn 双路径 reject,失败归约语义不清                                                                               | `filterOrElse(throwingP, throwingE)(ar)`      |
| `flatten`              | Ready for Product | D4       | 嵌套 `AsyncResult<AsyncResult<T, E>>` 展平,外层 Err 短路语义需确认;thunk 是否执行需明确                                                    | `flatten(of(okAR))`, `flatten(of(errAR))`     |
| `from`                 | Ready for Product | D3       | thunk 同步 throw 与 Promise reject 双路径,`.jules/sentinel.md` 中的 `in` 模式:若 `next` 是 `null` / primitive,`run in next` 抛 `TypeError` | `from(() => { throw }).run()` 与 `from(null)` |
| `fromPromise`          | Ready for Product | D3       | reject 映射 Err,thunk 同步 throw 不被包装;errFn 默认缺位透传 `unknown`                                                                     | 边界测试                                      |
| `fromResult`           | Ready for Product | D2       | 同步 `IResultOfT` 提升为 `AsyncResult`,`run()` 应等价于 `Promise.resolve(result)` — 但若 result 包含 `Error` 子类,Promise 序列化字段需确认 | 类型测试                                      |
| `map`                  | Ready for Product | D3       | 同步 fn throw 归约 Err;回调是 Sync / Async 双形态 — curried 形态推断分歧                                                                   | `map(throwingFn, okAR)`                       |
| `mapAsync`             | Ready for Product | D3       | 异步 fn reject 归约 Err;同步 throw 不被包装                                                                                                | `mapAsync(rejectingP, okAR)`                  |
| `mapErr`               | Ready for Product | D3       | 同步 fn throw 归约到原 Err 还是新 Err?语义需确认                                                                                           | `mapErr(throwingFn, errAR)`                   |
| `mapErrAsync`          | Ready for Product | D3       | 同上,异步 fn reject 路径                                                                                                                   | `mapErrAsync(rejectingP, errAR)`              |
| `match`                | Ready for Product | D4       | onOk / onErr 同步 / 异步混用推断退化;返回值类型需要 await 契约声明                                                                         | 类型测试                                      |
| `orElse`               | Ready for Product | D3       | errFn 是 `Promise<IResultOfT>` / 同步 `IResultOfT` 双形态;`run in` 边界                                                                    | `orElse(throwingFn, errAR)`                   |
| `orTee`                | Ready for Product | D3       | 失败侧 side-effect 同步 / 异步混用,throw 归约行为                                                                                          | 边界测试                                      |
| `swap`                 | Ready for Product | D4       | 交换 Ok/Err 后类型推断保留,若 `AsyncResult<T, E>` → `AsyncResult<E, T>` 是单向还是循环需声明                                               | 类型测试                                      |
| `tap`                  | Ready for Product | D3       | 同步 side-effect throw 归约 Err;原 Err 时不调用                                                                                            | `tap(throwingFn, okAR)`                       |
| `tapAsync`             | Ready for Product | D3       | 异步 side-effect reject 归约 Err;同步 throw 不被包装                                                                                       | `tapAsync(rejectingP, okAR)`                  |
| `tapErr`               | Ready for Product | D3       | 失败侧 side-effect throw 归约原 Err 还是新 Err?语义需明确                                                                                  | `tapErr(throwingFn, errAR)`                   |
| `tapErrAsync`          | Ready for Product | D3       | 同上,异步路径                                                                                                                              | `tapErrAsync(rejectingP, errAR)`              |
| `unwrapOr`             | Ready for Product | D4       | 默认值类型 / `T` 类型无约束,`def` 任意类型可能破坏 narrow 推断                                                                             | `unwrapOr('x', okAR(42))`                     |


## combine (3)

| 函数                   | 当前标注          | 风险维度 | 预期生产问题                                                                                                                                | 推荐验证手段                 |
| ---------------------- | ----------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| `combine`              | Ready for Product | D2       | 数组内任一 failure 后短路,语义但写入惯用法;奇偶 length 推断;空数组归约行为(`Ok([])` 还是归约某初始值)                                       | 空数组 / 单元素 / 多元素综合 |
| `all`                  | Ready for Product | D4       | 处理异构元组时,元素错误类型必须可对齐;`never` 传播路径上如果用户写 `all<IResultOfT<number, never>, IResultOfT<string, never>>()` 推断会跑偏 | TS 类型测试编译通过          |
| `combineWithAllErrors` | Ready for Product | D2       | 错误累积数组无深度上限,大量错误时内存无上限;空数组归约语义;错误项顺序稳定性                                                                 | 1000 失败项累积测试          |


## composition (5)

| 函数            | 当前标注          | 风险维度 | 预期生产问题                                                                                                                                                                                                                                                                                   | 推荐验证手段                                 |
| --------------- | ----------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| `pipe`          | Ready for Product | D4       | 1–10 overload,长链路下 IDE hover 退化为签名 N;10 步后 `(r) => …` 返回类型推断退化,用户复制管道后类型丢失                                                                                                                                                                                       | 11 步管道推断测试                            |
| `pipeAsync`     | Ready for Product | D4       | 同上,且返回 `Promise<U>`,大管道类型衰减更严重;混用 `Promise` 与 `AsyncResult` 时返回类型退化                                                                                                                                                                                                   | 类型测试                                     |
| `composeK`      | Ready for Product | D4       | Kleisli 组合;10 步骤以上复合后签名推断可能超 TS 递归深度,IDE hover 失败                                                                                                                                                                                                                        | 11 步组合推断测试                            |
| `composeKAsync` | Ready for Product | D4       | 与 `composeK` 同;`await` 顺序假设在并发场景下是否成立                                                                                                                                                                                                                                          | 类型 + 时序测试                              |
| `safeTry`       | Ready for Product | D3       | **直接对应 `.jules/sentinel.md` 第一条**。`* @note Ready for Product` 标的是当前已修版本,但仍需复算:短路时若 `iterator.return()` 未在所有失败分支调用,`finally` 不执行;sentinel 列举过的漏洞,需要在 `todo.md` 中标记为"持续监控"。校验 `yield*` 对 `err` 提前 return 时是否调用 `gen.return()` | 注入"错误路径"的资源(模拟打开句柄)并验证关闭 |


## operators (32)

| 函数              | 当前标注          | 风险维度 | 预期生产问题                                                                                                     | 推荐验证手段                                                                                                                    |
| ----------------- | ----------------- | -------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `and`             | Ready for Product | D2       | 数据后置形态,first failure 短路;两参数错误类型需可对齐,`never` 传播路径                                          | 类型测试                                                                                                                        |
| `andTee`          | Ready for Product | D2       | success 侧 side-effect 同步 throw 冒泡(契约与 `tap` 是否一致需确认);返回值是否包含 side-effect 结果              | `andTee(throwingFn)(...)`                                                                                                       |
| `andThrough`      | Ready for Product | D2       | 与 `andTee` 区别:side-effect 返回 `Result` 时窄化错误通道;回调 throw 归约语义                                    | `andThrough(throwingFn)(...)`                                                                                                   |
| `ap`              | Ready for Product | D4       | Applicative Apply;`IResultOfT<(a:A)=>B, E1>` 应用到 `IResultOfT<A, E2>` 时 `E1                                   | E2` 推断对齐;空 `ap`(`ok(undefined)`) 边界                                                                                      | 类型测试 + 边界   |
| `bimap`           | Ready for Product | D2       | 同步双回调 throw 时优先级未声明;若 okFn throw 时是否进入 errFn                                                   | `bimap(throwingOk, throwingErr)(errR)`                                                                                          |
| `bind`            | Ready for Product | D1       | 错误扩到 `E                                                                                                      | F`;多次链式后类型在 IDE 内极大串,可能触发 TS 递归深度限制;callback 是 `Result<B, F>` 而非 `Result<B, never>`,curried 形态需保留 | 10 步链式推断测试 |
| `contains`        | Ready for Product | D4       | 比较语义依赖 `Object.is` 还是 `===`,`NaN` / `-0` 行为                                                            | `contains(NaN, ok(NaN))`                                                                                                        |
| `exists`          | Ready for Product | D4       | 同步 predicate throw 归约 false,实际契约与 `filter` 不一致                                                       | 类型测试                                                                                                                        |
| `expect`          | Ready for Product | D4       | panic 时 `msg` 与 error 透传文案不一致;发布版本无 `__DEV__` 闸门;`throw new Error(msg)` 不带 `result.error`      | `expect('msg', errR)` 异常内容断言                                                                                              |
| `expectErr`       | Ready for Product | D4       | 与 `expect` 对偶                                                                                                 | 同上                                                                                                                            |
| `filterOrElse`    | Ready for Product | D2       | errFn curried 形态下 throw 归约;与 `filter` 不一致                                                               | `filterOrElse(throwingPred, throwingErrFn)`                                                                                     |
| `flatten`         | Ready for Product | D4       | 嵌套 `IResultOfT<IResultOfT<A, E>, E>` 展平,外层 Err 短路语义                                                    | `flatten(ok(err('x')))`, `flatten(err('x'))`                                                                                    |
| `map`             | Ready for Product | D1       | `map` 已用 `try/catch` 兜底,跨模块边界若 callback throw 后类型从 `F` 扩到 `unknown`,下游 `match` exhaustive 失败 | `map(throwingFn, okR)` 错误类型                                                                                                 |
| `mapErr`          | Ready for Product | D2       | failure 侧 callback throw 透传还是被吞?与 `map` 不对称                                                           | `mapErr(throwingFn, errR)`                                                                                                      |
| `mapOr`           | Ready for Product | D4       | `def` 默认值类型 / `T` 不协变时 narrow 失败                                                                      | 类型测试                                                                                                                        |
| `mapOrElse`       | Ready for Product | D4       | `onErr` 与 `fn` 同步 throw 归约优先级未声明                                                                      | `mapOrElse(throwingOnErr, throwingFn)`                                                                                          |
| `match`           | Ready for Product | D4       | onOk / onErr 同步 / 异步混用推断退化为 `unknown`;函数签名返回类型需明确文档                                      | 类型测试                                                                                                                        |
| `or`              | Ready for Product | D2       | 短路语义:`err` 时返回 second;两参数错误类型需可对齐,`never` 传播                                                 | 类型测试                                                                                                                        |
| `orElse`          | Ready for Product | D2       | errFn 返回 `IResultOfT<A, F>` 时,A 必须保持原样 — 失协变问题                                                     | 类型测试                                                                                                                        |
| `orTee`           | Ready for Product | D2       | failure 侧 side-effect 同步 throw 透传                                                                           | 边界测试                                                                                                                        |
| `orThrow`         | Ready for Product | D4       | `E extends Error` 约束;泛型 E 形参是否能推到 `Error` 子类需验证,非 Error 类型用户期望落空                        | `orThrow<string>(err('x'))`                                                                                                     |
| `orThrowWith`     | Ready for Product | D4       | curried `errorFn` 接收什么类型?是否限制 `Error` 子类?错误映射契约                                                | 类型测试                                                                                                                        |
| `separate`        | Ready for Product | D2       | 划分 ok / err 数组时,空数组归约;顺序稳定性                                                                       | 边界测试                                                                                                                        |
| `swap`            | Ready for Product | D4       | 双向交换?`swap(swap(r)) === r` 不变式需声明                                                                      | 不变式测试                                                                                                                      |
| `tap`             | Ready for Product | D2       | success 侧 side-effect throw 归约 Err(若 `map` catch,tap 是否也 catch)                                           | `tap(throwingFn, okR)`                                                                                                          |
| `tapErr`          | Ready for Product | D2       | 失败侧 side-effect throw 透传还是 catch                                                                          | 边界测试                                                                                                                        |
| `traverseArray`   | Ready for Product | D2       | 在长数组上某项同步 throw 且未被 ok/err 包装时,已存在 err 项之外再继续展开可能与"短路"语义冲突;空数组归约         | 边界测试                                                                                                                        |
| `unsafeUnwrap`    | Ready for Product | D4       | `throw e`(不带 message) 不利于日志;无 `__DEV__` 闸门                                                             | `unsafeUnwrap(errR)` 异常内容                                                                                                   |
| `unsafeUnwrapErr` | Ready for Product | D4       | `throw value` 时 value 为 plain object 栈化为空                                                                  | 同上                                                                                                                            |
| `unwrap`          | Ready for Product | D4       | panic 文案与 error 透传文案不一致;无 `__DEV__` 闸门                                                              | `unwrap(errR)`                                                                                                                  |
| `unwrapErr`       | Ready for Product | D4       | panic 文案;与 `unwrap` 命名一致                                                                                  | 同上                                                                                                                            |
| `unwrapOr`        | Ready for Product | D4       | 默认值类型 / T 不协变时 narrow 失败                                                                              | 类型测试                                                                                                                        |
| `unwrapOrElse`    | Ready for Product | D4       | errFn 同步 throw 归约语义                                                                                        | `unwrapOrElse(throwingFn, errR)`                                                                                                |


## option (16)

| 函数        | 当前标注          | 风险维度 | 预期生产问题                                                                                                                                 | 推荐验证手段                                      |
| ----------- | ----------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| `all`       | Ready for Product | D4       | 元组 `IOption<T>[]` 到 `IOption<[T]>` 推断对齐;空数组归约 `IOption<[]>`                                                                      | 类型 + 边界测试                                   |
| `bind`      | Ready for Product | D1       | `Option<U>` 链式 None 透传(此处 None 即"err"侧);若回调返回 `Option<U>`,curried 形态需保留推断                                                | 类型测试                                          |
| `contains`  | Ready for Product | D4       | 比较语义依赖 `Object.is` 还是 `===`                                                                                                          | `contains(NaN, ofSome(NaN))`                      |
| `filter`    | Ready for Product | D2       | predicate throw 归约 `None`,丢失错误对象;与 `Result.filterOrElse` 不对称                                                                     | `filter(throwingPred, ofSome(1))`                 |
| `flatten`   | Ready for Product | D4       | 嵌套 `IOption<IOption<T>>` 展平;外层 None 时短路语义                                                                                         | `flatten(ofNone())`, `flatten(ofSome(ofSome(1)))` |
| `map`       | Ready for Product | D4       | 同步 fn throw 归约 None,与 `Result.map` 的 try/catch 不对称(协议需确认)                                                                      | `map(throwingFn, ofSome(1))`                      |
| `match`     | Ready for Product | D4       | onSome / onNone 同步 / 异步混用推断退化为 `unknown`;async 重载若未导出需明确                                                                 | 类型测试                                          |
| `ofNone`    | Ready for Product | D4       | 静态单例还是每次新对象?若单例,`tap(option)` 等 side-effect 会在模块初始化时仅触发一次                                                        | `Object.is(ofNone(), ofNone())` 断言              |
| `ofSome`    | Ready for Product | D4       | 接受 `null` / `undefined` 时:`ofSome(undefined)` 是合法 `Some<undefined>` 还是归约 `None`?依赖文档                                           | `ofSome(null)` vs `ofSome(undefined)`             |
| `okOr`      | Ready for Product | D4       | `IOption<T>` 转 `IResultOfT<T, E>` 时,`E` 形参需显式;None 侧 error 是否固定还是每次新对象                                                    | 类型测试                                          |
| `okOrElse`  | Ready for Product | D4       | errFn curried 形态下 throw 透传还是 catch?与 `Result.fromPredicate` 不对称                                                                   | `okOrElse(throwingFn, ofNone())`                  |
| `orElse`    | Ready for Product | D4       | None 侧 fn 同步 throw 归约 None 还是冒泡?契约需确认                                                                                          | 边界测试                                          |
| `tap`       | Ready for Product | D2       | Some 侧 side-effect throw 归约 None;与 `Result.tap` 不对称                                                                                   | `tap(throwingFn, ofSome(1))`                      |
| `transpose` | Ready for Product | D1       | 处理 `IOption<IResultOfT<T,E>>` 时,双层判别联合 narrow;用户忘记 `isSome && inner.isSuccess` 会出类型漏洞。SPEC.md 未列在主 API 表 — 文档同步 | 类型穷尽测试                                      |
| `unwrapOr`  | Ready for Product | D4       | 默认值类型 / `T` 不协变时 narrow 失败                                                                                                        | 类型测试                                          |
| `zipWith`   | Ready for Product | D4       | 两 Option zip 时,任一 None 则归约 None;zipFunction throw 透传还是 catch                                                                      | `zipWith(throwingFn, ofSome(1), ofSome(2))`       |


## types (5) — 未标记 / 仅类型

| 函数          | 当前标注        | 风险维度 | 预期生产问题                                                                                                                                                                                                                                                         | 推荐验证手段                                                                  |
| ------------- | --------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `IResult`     | 未标记 / 仅类型 | D3       | JSON 反序列化重新水合后,字面量 `true` / `false as const` 退化为宽 `boolean`,narrowing 静默失效 — **必须用 `isSuccess: result.isSuccess === true` 恒等比较**,否则编译器无法分辨 `success` 与 `{isSuccess: 1}`。`TError` 默认值 `Error` 与 plain object 不匹配         | `JSON.parse(JSON.stringify({isSuccess:true,isFailure:false}))` 断言字面量退化 |
| `IResultOfT`  | 未标记 / 仅类型 | D3       | 同 `IResult`,加: `value` / `error` 字段在跨进程传递(JSON / postMessage / structuredClone)后丢失 `readonly` 标记,mutation 不会被类型系统捕获;`IResultFailure.error: TError` 默认 `Error`,用户传 `{kind: ''NotFound''}` 时下游 `instanceof Error` 检查失败             | `Object.freeze` 后 mock mutation 测试                                         |
| `Option`      | 未标记 / 仅类型 | D3       | 同 `IResult` 的字面量退化问题;`IOptionSome.value` readonly 在序列化后丢失                                                                                                                                                                                            | 同上                                                                          |
| `AsyncResult` | 未标记 / 仅类型 | D3       | `run: () => Promise<IResultOfT>` 是裸函数,thunk 同步 throw 时 Promise 状态不一致;调用方若误用 `try/catch` 包 `run()` 会捕获同步抛错而非异步 reject,行为不一致。`.jules/sentinel.md` 第二条提到 `in` 模式的安全性,接口层无解 — 应在类型层注释此契约(并在实现层硬约束) | `from(() => { throw }).run()` 时序测试                                        |
| `AsyncOption` | 未标记 / 仅类型 | D3       | 同 `AsyncResult`,目标 `IOption<T>`;thunk 同步 throw / reject 双通道契约                                                                                                                                                                                              | 同上                                                                          |

